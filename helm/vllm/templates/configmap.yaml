apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vllm.fullname" . }}-config
  labels:
    {{- include "vllm.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.vllm.config | nindent 4 }}
