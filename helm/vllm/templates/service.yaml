apiVersion: v1
kind: Service
metadata:
  name: {{ include "vllm-host.fullname" . }}
  labels:
    {{- include "vllm-host.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "vllm-host.selectorLabels" . | nindent 4 }}
