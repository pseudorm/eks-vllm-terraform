apiVersion: v1
kind: ConfigMap
metadata:
  name: locust-script
  namespace: load-test
data:
  locustfile.py: |
    # This will be populated from your local locustfile_claude_code.py
    # Use the apply script to inject it
